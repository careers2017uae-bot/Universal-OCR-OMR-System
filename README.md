# Universal-OCR-OMR-System
This solution correctly combines:  OCR (text extraction)  Handwriting recognition (where supported by model)  Language-agnostic embeddings &amp; reasoning via Jina AI  Jina is used for language-agnostic understanding, cleanup, layout recovery, and semantic reconstruction, not raw OCR pixels.
